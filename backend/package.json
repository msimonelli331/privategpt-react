{
  "name": "privategpt-react-backend",
  "version": "1.0.0",
  "description": "This is a Node.js backend server that provides a REST API for interacting with Kubernetes clusters. It's designed to be used with the PrivateGPT React application to provide Kubernetes namespace and pod information.",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "node src/index.js",
    "start": "node src/index.js",
    "build": "cd ../frontend && pnpm install && pnpm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@kubernetes/client-node": "^1.4.0",
    "express": "^5.2.1"
  }
}
